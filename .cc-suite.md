# CC-Suite Configuration

Project-specific settings for cc-suite commands.
Generated by `/cc-suite:init`. Edit freely — all fields are optional.

## Project

- **Stack**: Python 3.11+ (stdlib only for `bin/nlpm-check`, PyYAML for vocab tooling) + Node.js (VitePress site under `site/`)
- **Test command**: `python3 -m unittest tests.test_nlpm_check tests.test_nlpm_badge tests.test_exemplar_helpers`
- **Source directories**: `bin/`, `commands/`, `agents/`, `skills/`, `hooks/`, `auditor/scripts/`, `auditor/`, `site/`

## Defaults

These override the built-in defaults for all commands in this project.
Remove a line to fall back to the built-in default.

- **Default model**: gpt-5.5
- **Default effort**: high
- **Default audit type**: mini
- **Default sandbox**: workspace-write

## Audit Focus

balanced

Additional instructions appended to every audit's developer-instructions:

```text
Give equal attention to all audit dimensions.
```

## Skip Patterns

Files and directories to always skip during audits (glob patterns):

```text
node_modules/
site/.vitepress/dist/
site/.vitepress/cache/
site/public/
site/case-studies/
site/reports/
site/.vitepress/theme/data/
dist/
build/
coverage/
*.min.js
*.bundle.js
*.lock
vendor/
.git/
auditor/audits/
auditor/reports/
auditor/exemplars/
auditor/findings.jsonl
auditor/disagreements.jsonl
auditor/logs/
auditor/vocab-advisories.jsonl
analysis/vocabulary-extract/
.nlpm-test/
```

## Project-Specific Instructions

Custom instructions appended to Codex's developer-instructions for every command.

```text
This is NLPM — a Claude Code plugin (Natural Language Programming Manager).
The repo has two strict scopes (P1 of the vocabulary principles):
  - internal: commands/, agents/, skills/, hooks/ — the plugin surface that
    runs locally in a user's repo.
  - auditor: .github/workflows/auditor-*.yml + auditor/ — the GHA pipeline
    that audits public Claude Code plugins.
Cross-scope homonyms (scan, test, discover) are sanctioned in
skills/nlpm/vocabulary/registry.yaml — do not flag them as drift.

Audit conventions:
  - `bin/nlpm-check` is stdlib-only Python by contract. Do NOT propose
    third-party dependencies for it.
  - `auditor/scripts/*.sh` use POSIX shell + jq. Keep them portable.
  - The VitePress site under `site/` is a thin presentation layer; treat
    its content as canonical-source mirrors, not edit targets.
  - Append-only logs (`auditor/findings.jsonl`, `disagreements.jsonl`,
    `logs/events.jsonl`, `vocab-advisories.jsonl`) must never be rewritten
    by an audit fix — flag, do not edit.
  - Protected paths (`skills/`, `agents/`, blocked by guard-protected-paths
    .sh) require human review; surface findings, do not auto-edit.

The plugin dogfoods its own rules: NLPM's self-audit score is 100/100 and
should stay there. Use R01–R51 (skills/nlpm/rules/SKILL.md) as the rubric
for any NL-artifact finding.
```
